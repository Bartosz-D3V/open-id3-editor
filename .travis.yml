dist: trusty
sudo: required

language: node_js

node_js:
  - '8.10'

cache:
  yarn: true
  directories:
    - ./node_modules
    - $HOME/.cache/yarn
    - $HOME/.npm

matrix:
  fast_finish: true

install:
  - yarn

script:
  - yarn lint
  - yarn test

notifications:
  email:
    on_success: never
    on_failure: change
